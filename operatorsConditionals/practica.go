package main
import (
	"fmt"
)
func main() {
	// Practica de operadores y condicionales en Go
	// Dentro de nuestro codigo vamos a tener 
	//2 variables definidas, que van a ser:  licencia y edad

	licence := true
	edad := 15
	
	if edad > 15 && licence {
		fmt.Println("Puede seguir avanzando")
	}else{
		fmt.Println("No puede seguir avanzando")
	}


//🔥 DESAFÍO RELÁMPAGO (15 min)
//Como es tu primer día, te doy un mini-desafío de calentamiento:
//Problema: Crea variables para representar un envío con: 
// ID (entero), destino (string), peso (decimal), y entregado (booleano). 
// Usa la sintaxis más corta posible de Go.



}